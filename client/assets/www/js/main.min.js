require.config({ 
    //path mappings for module names not found directly under baseUrl 
    paths: { 
        jquery:  'vendor/jquery-1.9.1.min', 
        jqm:     'vendor/jquery.mobile-1.3.0.min', 
        underscore: 'vendor/underscore.min', 
        backbone:   'vendor/backbone.min', 
        text:       'vendor/require.text',
        cs  :       'vendor/require.cs',
        coffeescript: 'vendor/coffee-script.min', 
        
        view:       'view', 
        model:      'model' 
    },

	shim: {
		'backbone': {
            //These script dependencies should be loaded before loading
            //backbone.js
            deps: ['underscore', 'jquery']
	}
}); 
 
//1. load app.js, 
//2. configure jquery mobile to prevent default JQM ajax navigation 
//3. bootstrapping application
define(['jquery', 'jquery.overload'], function() { 
	//Loading jquery.overload
});

define(['app','jqm.config'], function(app) { 
    $(document).ready(function() { 
      console.log('DOM IS READY');// Handler for .ready() called. 
    });    
    app.initialize(); 
});